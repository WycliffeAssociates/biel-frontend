---
import {getDict, type i18nDictType} from "@src/i18n/strings";
import Layout from "@layouts/LayoutNoFooter.astro";
import {HeaderMenu} from "@components/HeaderMenu";
import {getMenus, getLanguagesPageSlugs, getWpmlLanguages} from "@src/data/wp";
import {BreadCrumbs} from "@components/BreadCrumbs";
import ResourceIndex from "@components/ResourcesPage/ResourcesPage.astro";
import type {languageType, Menu} from "@customTypes/types";
import {capitalizeFirstLetter, titleCase} from "@src/utils";

type Props = {
  title: string;
  allLangs: languageType[];
  langInfo: languageType;
  headerMenu: Menu;
  pageDict: i18nDictType;
  crumbs: {
    uri: string;
    slug: string;
    title: string;
  }[];
};

export async function getStaticPaths() {
  const gqlUrl: string =
    Astro.locals?.runtime?.env.WORDPRESS_GQL_URL ||
    import.meta.env.WORDPRESS_GQL_URL ||
    process.env.WORDPRESS_GQL_URL;
  const restUrl: string =
    Astro.locals?.runtime?.env.WORDPRESS_REST_MENU_ENDPOINT ||
    import.meta.env.WORDPRESS_REST_MENU_ENDPOINT ||
    process.env.WORDPRESS_REST_MENU_ENDPOINT;
  const [wpmlLangDict, menus, languagePageSlugs] = await Promise.all([
    getWpmlLanguages({gqlUrl}),
    getMenus({restUrl}),
    getLanguagesPageSlugs({gqlUrl}),
  ]);
  const nonHiddenLanguages = new Set(Object.keys(menus));

  const langSwitcherList = Object.values(wpmlLangDict)
    .filter((lang) => {
      return nonHiddenLanguages.has(lang.code);
    })
    .map((lang) => {
      const copy = {...lang};
      const matchingSlug = languagePageSlugs.data.page.translations.find(
        (t) => t.languageCode === lang.code
      );
      if (lang.code === "en") {
        copy.localizedUrl = "/resources/languages";
      } else {
        copy.localizedUrl = `${matchingSlug?.uri}`;
      }
      return copy;
    });
  languagePageSlugs.data.page.translations.push({
    languageCode: "en",
    slug: languagePageSlugs.data.page.slug,
    title: languagePageSlugs.data.page.title,
    uri: languagePageSlugs.data.page.uri,
  });
  const pageData = languagePageSlugs.data.page.translations
    .filter((t) => nonHiddenLanguages.has(t.languageCode))
    .map((t) => {
      const slug = t.uri;
      // t.languageCode === "en" ? `${t.slug}` : `/${t.languageCode}/${t.slug}`;

      const langInfo = wpmlLangDict[t.languageCode] || wpmlLangDict.en!;
      const headerMenu =
        menus[langInfo.language_code]?.["header-menu"] ||
        menus.en?.["header-menu"]!;
      const crumbs = slug
        .split("/")
        .filter((v) => !!v)
        .map((crumb, i) => {
          const uri = `/${slug
            .split("/")
            .filter((v) => !!v)
            .slice(0, i + 1)
            .join("/")}`;
          return {
            uri: uri,
            slug: "",
            title: capitalizeFirstLetter(crumb),
          };
        });

      return {
        params: {
          resourceIndex: slug,
        },
        props: {
          title: t.title,
          allLangs: langSwitcherList,
          langInfo,
          headerMenu,
          pageDict: getDict(t.languageCode, true)!,
          crumbs,
        },
      };
    });

  return pageData;
}

const {title, allLangs, langInfo, headerMenu, pageDict, crumbs} = Astro.props;

// resource/languages
// es/recursos/idiomas
const ogImage = `${Astro.site?.origin}/icon-512.png`;
const {resourceIndex: prefix} = Astro.params;
---

<Layout
  title={titleCase({
    lang: langInfo.code,
    str: `${title} ${pageDict.siteName}`,
  })}
  inlineCss={[]}
  description="Resources Languages Page"
  langInfo={langInfo}
  langSwitcher={allLangs}
  langDirection="ltr"
  siteName={pageDict.siteName}
  ogImage={ogImage}
>
  <HeaderMenu
    allLangs={allLangs}
    currentLang={langInfo}
    menu={headerMenu}
    i18nDict={pageDict}
    client:load
  />
  <BreadCrumbs crumbs={{nodes: crumbs}} />

  <ResourceIndex
    server:defer
    i18nDict={pageDict}
    detailPrefix={prefix}
    siteIetf={langInfo.code}
  >
    <div
      slot="fallback"
      class="min-h-screen w-full place-content-center text-center"
    >
      {pageDict.ls_Loading}
    </div>
  </ResourceIndex>
</Layout>
